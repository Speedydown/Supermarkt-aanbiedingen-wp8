<Page
    x:Class="Supermarkt_aanbiedingen.ConfigureSupermarkets"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Supermarkt_aanbiedingen"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Supermarkt_aanbiedingenLogic.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid Margin="5,0,5,0" Visibility="Collapsed" x:Name="ContentGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Welkom!" FontSize="55" Margin="0,0,0,0"/>
            <TextBlock Grid.Row="1" Text="Selecteer hieronder van welke supermarkten waar u aanbiedingen van wenst te zien." FontSize="18" TextWrapping="WrapWholeWords" Margin="0,0,0,5"/>
            <ListView x:ConnectionId='1' x:Name="SupermarktetsListview" Grid.Row="2"                                             IsItemClickEnabled="True">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                    </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="DarkBlue" BorderThickness="1" Margin="0,0,0,10">
                            <Grid x:Name="SupermarketGrid">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="25"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="25"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox x:Name="Checked" Margin="0,-15,0,0" IsChecked="{Binding SupermarketEnabled}" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" HorizontalAlignment="Left" Foreground="DarkBlue" VerticalAlignment="Top" Canvas.ZIndex="255"/>
                                
                                <Image Source="{Binding ImageURL}" Stretch="Fill" Width="60" Height="60" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
                                <TextBlock Text="{Binding Name}" Foreground="DarkBlue" FontWeight="Bold" FontSize="19" Grid.Row="0" Grid.Column="3"/>
                                <TextBlock Text="{Binding Slogan}" Foreground="DarkBlue" FontSize="15" Grid.Row="1" Grid.Column="3" TextWrapping="WrapWholeWords" VerticalAlignment="Top" Margin="0, -8,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button x:ConnectionId='2' x:Name="NextButton" Grid.Row="3" HorizontalAlignment="Right" Content="naar de aanbiedingen!" IsEnabled="False"                         />
        </Grid>
        <Grid x:Name="LoadingGrid" Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="1" Text="De aanbiedingen komen er aan!" Foreground="DarkBlue" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,15"/>
            <controls:ProgressRing Grid.Row="2" Height="250" Width="250"  Visibility="Visible" IsActive="True" Margin="0,0,0,10"/>
            <TextBlock Grid.Row="3" Text="Een moment geduld AUB...." Foreground="DarkBlue" FontSize="14" HorizontalAlignment="Center"/>
        </Grid>
        
    </Grid>
</Page>

